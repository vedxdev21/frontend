'use client';
import { useDispatch, useSelector } from 'react-redux';

// Pre-typed hooks for convenience
export const useAppDispatch = () => useDispatch();
export const useAppSelector = useSelector;
